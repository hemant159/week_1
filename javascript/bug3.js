const add = (a, b) => {
 return  a + b;          //previously it was written as a + b, which is incorrect. Now it is corrected to return a + b, which will return the sum of a and b.

};

console.log(add(2, 3));